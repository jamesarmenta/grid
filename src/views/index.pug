head
  title Grid 
  link(rel='stylesheet', href='/css/main.css', type='text/css')
body
  #container
    .on-board
     p Click and drag create. Click a line to delete. Right-click for colors.
    #canvas_wrapper
      canvas#interactive
      canvas#artboard
      canvas#base
    .about
     p Send this page to a friend and #[wbr]watch changes in real-time. 
     br
     p (This project is still in the works - #[wbr]come back soon to see more features). 
  script
    if data
        | var serverData = !{JSON.stringify(data)}
  script(src='/bundle.min.js')
  //- Firebase should be parsed last
  script(src='https://www.gstatic.com/firebasejs/4.3.0/firebase.js')
